<p>You can control the component directly from the controller by target the component in your actions. Try changing the YouTube ID or tap the buttons below.</p>

<p>{{input value=youTubeId}}</p>
<menu>
	<button {{action "togglePlay" target="myPlayer"}}>
		{{#if myPlayer.isPlaying}}Pause{{else}}Play{{/if}}
	</button>
	<button {{action "toggleVolume" target="myPlayer"}}>
		{{#if myPlayer.isMuted}}Unmute{{else}}Mute{{/if}}
	</button>
</menu>

<h2>components/ember-youtube</h2>
{{ember-youtube ytid=youTubeId
	name=myPlayer
	debug=false
	showControls=true
	showTime=true
	showProgress=true
	playing="ytPlaying"
	paused="ytPaused"
	ended="ytEnded"}}
